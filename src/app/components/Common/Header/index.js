import React from 'react';
import './style.css';

const Header = (props) => {
    return (
        <header>
            <h1>The Brewery - Transport Refrigeration Sensors</h1>
        </header>
    );
}

export default Header;